// Intentional release hold. Enable only after provider approval and end-to-end QA.
// Backend activation is separate and remains fail-closed by default.
export const INTEGRATION_COLLECTIONS_ENABLED = false;
